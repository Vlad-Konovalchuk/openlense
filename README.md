# OpenLense

Universal Open Data Aggregator

This monorepo contains both frontend and backend applications.

## Structure
- `apps/frontend` — React + TypeScript + TailwindCSS
- `apps/backend` — FastAPI + PostgreSQL
- `packages` — Shared code (types, utils, etc.)
- `docs` — Specifications and documentation
